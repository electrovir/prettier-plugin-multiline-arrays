import {Doc} from 'prettier';

export function insertLinesIntoArguments(
    inputDoc: Doc,
    forceWrap: boolean,
    lineCounts: number[],
    wrapThreshold: number,
    debug: boolean,
): Doc {
    return inputDoc;
}
